runtime: python311

env_variables:
  SECRET_KEY: "simple-test-key"

handlers:
- url: /static
  static_dir: static
  secure: always

- url: /.*
  script: simple_app.app
  secure: always

automatic_scaling:
  min_instances: 1
  max_instances: 2